{
  "name": "voice-sdk",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "build:packages": "turbo build --filter='./packages/**'",
    "dev": "turbo dev",
    "dev:iframe": "turbo run dev --filter='./packages/**' --filter=example-vite-app-embed",
    "lint": "oxlint --disable-nested-config --type-aware --deny-warnings --report-unused-disable-directives . && pnpm lint:contracts",
    "lint:contracts": "node --test tools/lint-contract/config.test.mjs",
    "typecheck": "turbo typecheck",
    "typecheck:deps": "turbo typecheck:deps",
    "test": "turbo test",
    "test-watch": "turbo test-watch",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "finalize-types": "node tools/finalize-types.mjs",
    "package:smoke": "node tools/check-package-exports.mjs",
    "check:fallow": "fallow dead-code --type-aware --type-aware-require best-effort --fail-on-issues",
    "check:fallow:private-types": "fallow dead-code --private-type-leaks --type-aware --type-aware-require best-effort",
    "check:fallow:audit": "fallow audit --gate new-only --type-aware --type-aware-require best-effort",
    "check:deps": "syncpack lint",
    "check:docs": "cspell --dot --no-progress --no-summary \"**/*.{cjs,css,js,json,jsonc,md,mjs,mts,ts,tsx,yaml,yml}\" && markdownlint-cli2",
    "check:packages": "pnpm build:packages && pnpm --recursive --filter='./packages/**' package:check",
    "api-report:check": "pnpm build:packages && pnpm --recursive --filter='./packages/**' api-report:check",
    "api-report:update": "pnpm build:packages && pnpm --recursive --filter='./packages/**' api-report:update",
    "check": "pnpm build && pnpm lint && pnpm typecheck && pnpm typecheck:deps && pnpm format:check && pnpm check:deps && pnpm check:docs && pnpm check:fallow && pnpm check:packages && pnpm api-report:check && pnpm test",
    "release": "pnpm build:packages && pnpm check:packages && pnpm publish -r --access public --no-git-checks"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "0.18.5",
    "@humeai/typescript-config": "workspace:*",
    "@microsoft/api-extractor": "7.59.0",
    "cspell": "10.1.1",
    "fallow": "3.19.0",
    "markdownlint-cli2": "0.23.2",
    "oxfmt": "0.65.0",
    "oxlint": "1.80.0",
    "oxlint-tsgolint": "7.0.2001",
    "publint": "0.3.24",
    "syncpack": "15.3.3",
    "turbo": "^2.9.14"
  },
  "browserslist": [
    "last 2 Chrome versions, last 2 iOS major versions, Firefox ESR, not dead"
  ],
  "engines": {
    "node": ">=22.13",
    "pnpm": "11.24.0"
  },
  "packageManager": "pnpm@11.24.0"
}
